model_config: configs/model.yaml
tokenizer_path: artifacts/tokenizer.json
dataset_path: data/public_balanced_reviewed.jsonl
base_checkpoint: checkpoints/public_v20_challenge_polish/latest.pt
output_dir: checkpoints/public_balanced_reviewed_v2
seed: 7119
validation_fraction: 0.1
batch_size: 4
gradient_accumulation_steps: 2
learning_rate: 0.00004
min_learning_rate: 0.000004
weight_decay: 0.01
epochs: 8
warmup_steps: 15
max_steps: 240
gradient_clip: 1.0
log_interval: 20
eval_interval: 60
eval_batches: 40
save_interval: 120
num_workers: 0
amp: true
