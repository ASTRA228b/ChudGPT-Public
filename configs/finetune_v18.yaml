model_config: configs/model.yaml
tokenizer_path: artifacts/tokenizer.json
dataset_path: data/public_v17_conversations.jsonl
base_checkpoint: checkpoints/public_v18_base/best.pt
output_dir: checkpoints/public_v18_sft
seed: 1819
validation_fraction: 0.08
batch_size: 6
gradient_accumulation_steps: 4
learning_rate: 0.000025
min_learning_rate: 0.000002
weight_decay: 0.01
epochs: 5
warmup_steps: 60
max_steps: 600
gradient_clip: 1.0
log_interval: 20
eval_interval: 100
eval_batches: 80
save_interval: 100
num_workers: 0
amp: true
resume_from: null
