model_config: configs/model.yaml
tokenizer_path: artifacts/tokenizer.json
dataset_path: data/public_v15_conversations.jsonl
additional_dataset_path: data/public_balanced_binding.jsonl
additional_dataset_repeat: 4
base_checkpoint: checkpoints/public_v20_challenge_polish/latest.pt
output_dir: checkpoints/public_broad_recovery_v1
seed: 20260907
validation_fraction: 0.05
batch_size: 8
gradient_accumulation_steps: 2
learning_rate: 0.000025
min_learning_rate: 0.0000025
weight_decay: 0.01
epochs: 2
warmup_steps: 30
max_steps: 900
gradient_clip: 1.0
log_interval: 30
eval_interval: 150
eval_batches: 50
save_interval: 300
num_workers: 0
amp: true
