model_config: configs/model.yaml
tokenizer_path: artifacts/tokenizer.json
dataset_path: data/public_general_retrain.jsonl
base_checkpoint: checkpoints/public_v20_challenge_polish/latest.pt
output_dir: checkpoints/public_general_retrain_v1
seed: 20260907
validation_fraction: 0.08
batch_size: 4
gradient_accumulation_steps: 2
learning_rate: 0.000015
min_learning_rate: 0.0000015
weight_decay: 0.01
epochs: 12
warmup_steps: 30
max_steps: 650
gradient_clip: 1.0
log_interval: 20
eval_interval: 100
eval_batches: 80
save_interval: 100
num_workers: 0
amp: true
batch_delay_ms: 120
