model_config: configs/model.yaml
tokenizer_path: artifacts/tokenizer.json
dataset_path: data/public_conversations.jsonl
base_checkpoint: checkpoints/base/best.pt
output_dir: checkpoints/chat
seed: 229
validation_fraction: 0.04
batch_size: 4
gradient_accumulation_steps: 8
learning_rate: 0.00005
min_learning_rate: 0.000005
weight_decay: 0.01
epochs: 4
warmup_steps: 100
max_steps: 1800
gradient_clip: 1.0
log_interval: 20
eval_interval: 100
eval_batches: 40
save_interval: 250
num_workers: 0
amp: true
resume_from: null
